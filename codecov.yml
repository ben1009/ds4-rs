coverage:
  status:
    project:
      default:
        informational: true
        only_pulls: false
    patch:
      default:
        informational: true

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
